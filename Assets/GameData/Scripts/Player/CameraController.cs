using UnityEngine;

public class CameraController : MonoBehaviour
{
    [SerializeField]
    private float speed = 12f;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
